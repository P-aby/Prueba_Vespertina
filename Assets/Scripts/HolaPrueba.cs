using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HolaPrueba : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hola a todos");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
